{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "preLaunchTask": "CMake: build",
            "target": "./squarebox",
            "cwd": "${workspaceRoot}/build",
            "valuesFormatting": "parseText"
        }
    ]
}